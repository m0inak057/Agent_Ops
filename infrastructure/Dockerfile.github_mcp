# Dockerfile for the github_mcp server.
FROM python:3.11-slim

WORKDIR /app

COPY mcp_servers/github_mcp/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY mcp_servers/github_mcp/ ./

EXPOSE 8001

CMD ["python", "server.py"]
