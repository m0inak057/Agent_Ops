"""MCP tool: search for files matching a name or content pattern.

Given a root path and a search pattern, returns matching file paths
within the local filesystem sandbox.
"""


async def search_files(path: str, pattern: str) -> dict:
    """Search for files under path matching the given pattern."""
    pass
