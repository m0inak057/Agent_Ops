"""MCP tool: create a commit with staged changes in a cloned repository.

Used by the developer agent to commit a proposed fix to a branch.
"""


async def create_commit(repository_path: str, message: str) -> dict:
    """Commit staged changes in the repository with the given message."""
    pass
