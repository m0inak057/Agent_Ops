"""MCP tool: search for a code pattern within a cloned repository.

Given a repository path and a search query, returns matching file
locations and surrounding context.
"""


async def search_code(repository_path: str, query: str) -> dict:
    """Search the repository for the given code pattern."""
    pass
