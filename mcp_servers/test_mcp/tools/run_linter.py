"""MCP tool: run a code linter against the repository.

Executes a linter (e.g. ruff) and returns style and correctness
violations for the code quality agent to analyze.
"""


async def run_linter(repository_path: str) -> dict:
    """Run the linter for the given repository and return violations."""
    pass
