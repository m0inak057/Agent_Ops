"""MCP tool: run the repository's test suite.

Executes the project's tests (e.g. via pytest) and returns pass/fail
results for the testing agent to analyze.
"""


async def run_tests(repository_path: str) -> dict:
    """Run the test suite for the given repository and return results."""
    pass
